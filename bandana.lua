Bandana = object:New({
	tag = 'bandana',
  marginx = 1,
  marginy = 1,
  animation = 'bandana'
})

function Bandana:setAcceleration(dt)
	--if self:touchPlayer() then  -- Kill player, if touching
	--	p.dead = true
  --end
end
